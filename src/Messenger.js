import React from "react";
import DefaultContainer from "./DefaultContainer";

const Messenger = () => {
  return <DefaultContainer text="You don't have any new messages." />;
};

export default Messenger;
